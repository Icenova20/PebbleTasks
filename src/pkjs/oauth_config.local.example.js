/**
 * Local dev — oauth_config.local.js is gitignored and written by:
 *   npm run decode-token -- 'PEBBLETASKS1…'
 *  `npm run setup-hooks` to install the hooks for the app (pre-commit clear out the file and re-build)
 *
 * Optional: set settingsBaseUrl to your LAN oauth-server (not localhost on the phone).
 */
module.exports = {
  hardcodedAccessToken: '',
  // settingsBaseUrl: 'http://192.168.1.10:3847',
};
